First, we will need to install aws cli and setup aws command - aws configure

Second, also need to check python and boto3 are working well in your machine.
pip install boto3
python3
import boto3

sudo pip3 install --upgrade awscli